rootProject.name = "pt.kotlintojs"

include(":annotations")
include(":processor")

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
